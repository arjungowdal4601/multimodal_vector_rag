{
  "chunk_1": {
    "content": "Recurrent models typically factor computation along the symbol positions of the input and output sequences. Aligning the positions to steps in computation time, they generate a sequence of hidden states h t , as a function of the previous hidden state h t -1 and the input for position t . This inherently sequential nature precludes parallelization within training examples, which becomes critical at longer sequence lengths, as memory constraints limit batching across examples. Recent work has achieved significant improvements in computational efficiency through factorization tricks [18] and conditional computation [26], while also improving model performance in case of the latter. The fundamental constraint of sequential computation, however, remains.\n\nAttention mechanisms have become an integral part of compelling sequence modeling and transduction models in various tasks, allowing modeling of dependencies without regard to their distance in the input or output sequences [2, 16]. In all but a few cases [22], however, such attention mechanisms are used in conjunction with a recurrent network.\n\nIn this work we propose the Transformer, a model architecture eschewing recurrence and instead relying entirely on an attention mechanism to draw global dependencies between input and output. The Transformer allows for significantly more parallelization and can reach a new state of the art in translation quality after being trained for as little as twelve hours on eight P100 GPUs.",
    "refreshed_content": "Traditional RNN-based sequence models compute hidden states sequentially over positions: each hidden state h_t depends on the previous state h_{t-1} and the input at position t. Because this computation is inherently step-by-step, it cannot be parallelized within a single training example, and this bottleneck worsens for long sequences where memory limits reduce batch sizes. Prior methods improve efficiency via factorization tricks [18] or conditional computation [26] (the latter also improving accuracy), but they do not remove the core sequential constraint.\n\nAttention mechanisms help model long-range dependencies regardless of distance in the sequence [2,16], yet they are usually paired with recurrent networks (with a few exceptions [22]).\n\nThe paper introduces the Transformer: a sequence transduction architecture that eliminates recurrence entirely and relies only on attention to capture global input–output dependencies, enabling much greater parallelism and achieving state-of-the-art translation quality with training as short as 12 hours on 8 P100 GPUs.",
    "source": [
      2
    ],
    "metadata_addition": {
      "pdf_name": "sample",
      "doc_assets_path": "sample_doc_assets"
    }
  },
  "chunk_2": {
    "content": "## 2 Background\n\nThe goal of reducing sequential computation also forms the foundation of the Extended Neural GPU [20], ByteNet [15] and ConvS2S [8], all of which use convolutional neural networks as basic building block, computing hidden representations in parallel for all input and output positions. In these models, the number of operations required to relate signals from two arbitrary input or output positions grows in the distance between positions, linearly for ConvS2S and logarithmically for ByteNet. This makes it more difficult to learn dependencies between distant positions [11]. In the Transformer this is reduced to a constant number of operations, albeit at the cost of reduced effective resolution due to averaging attention-weighted positions, an effect we counteract with Multi-Head Attention as described in section 3.2.\n\nSelf-attention, sometimes called intra-attention is an attention mechanism relating different positions of a single sequence in order to compute a representation of the sequence. Self-attention has been used successfully in a variety of tasks including reading comprehension, abstractive summarization, textual entailment and learning task-independent sentence representations [4, 22, 23, 19].\n\nEnd-to-end memory networks are based on a recurrent attention mechanism instead of sequencealigned recurrence and have been shown to perform well on simple-language question answering and language modeling tasks [28].\n\nTo the best of our knowledge, however, the Transformer is the first transduction model relying entirely on self-attention to compute representations of its input and output without using sequencealigned RNNs or convolution. In the following sections, we will describe the Transformer, motivate self-attention and discuss its advantages over models such as [14, 15] and [8].",
    "refreshed_content": "### Background (reducing sequential computation)\nConvolutional sequence models such as Extended Neural GPU [20], ByteNet [15], and ConvS2S [8] reduce sequential computation by using CNNs to compute representations for all positions in parallel. However, the computational path length needed to connect two positions grows with their distance: it is linear in ConvS2S and logarithmic in ByteNet, which can hinder learning long-distance dependencies [11].\n\nTransformer-style self-attention reduces this to a constant number of operations to relate any two positions. This comes with a tradeoff: attention averages over positions, reducing effective resolution; the model addresses this using Multi-Head Attention (described later in §3.2).\n\nSelf-attention (intra-attention) computes a sequence representation by relating different positions within the same sequence, and has worked well for reading comprehension, abstractive summarization, textual entailment, and task-independent sentence representations [4,22,23,19].\n\nEnd-to-end memory networks use a recurrent attention mechanism rather than sequence-aligned recurrence and perform well on simple-language QA and language modeling [28].\n\nThe authors claim the Transformer is the first transduction model to rely entirely on self-attention for both input and output representations, without sequence-aligned RNNs or convolutions, and will subsequently describe and motivate this approach while comparing against models like [14,15,8].",
    "source": [
      2
    ],
    "metadata_addition": {
      "pdf_name": "sample",
      "doc_assets_path": "sample_doc_assets"
    }
  },
  "chunk_3": {
    "content": "## 3 Model Architecture\n\nMost competitive neural sequence transduction models have an encoder-decoder structure [5, 2, 29]. Here, the encoder maps an input sequence of symbol representations ( x 1 , ..., x n ) to a sequence of continuous representations z = ( z 1 , ..., z n ) . Given z , the decoder then generates an output sequence ( y 1 , ..., y m ) of symbols one element at a time. At each step the model is auto-regressive [9], consuming the previously generated symbols as additional input when generating the next.\n\nThe Transformer follows this overall architecture using stacked self-attention and point-wise, fully connected layers for both the encoder and decoder, shown in the left and right halves of Figure 1, respectively.",
    "refreshed_content": "### Model architecture overview\nNeural sequence transduction systems commonly use an encoder–decoder design [5,2,29]. The encoder converts an input token sequence (x1,…,xn) into continuous representations z = (z1,…,zn). Conditioned on z, the decoder produces an output token sequence (y1,…,ym) one token at a time in an autoregressive manner [9], using previously generated tokens as additional input for predicting the next.\n\nThe Transformer keeps this encoder–decoder structure but implements both sides using stacks of self-attention plus position-wise fully connected (feed-forward) layers (illustrated in Figure 1 with encoder on the left and decoder on the right).",
    "source": [
      2
    ],
    "metadata_addition": {
      "pdf_name": "sample",
      "doc_assets_path": "sample_doc_assets"
    }
  },
  "chunk_4": {
    "content": "## 3.1 Encoder and Decoder Stacks\n\nEncoder: The encoder is composed of a stack of N = 6 identical layers. Each layer has two sub-layers. The first is a multi-head self-attention mechanism, and the second is a simple, position-\n\nwise fully connected feed-forward network. We employ a residual connection [10] around each of the two sub-layers, followed by layer normalization [1]. That is, the output of each sub-layer is LayerNorm( x +Sublayer( x )) , where Sublayer( x ) is the function implemented by the sub-layer itself. To facilitate these residual connections, all sub-layers in the model, as well as the embedding layers, produce outputs of dimension d model = 512 .",
    "refreshed_content": "### Encoder/decoder stacks (encoder portion and shared design choices)\nThe encoder consists of N = 6 identical layers. Each encoder layer contains two sublayers: (1) a multi-head self-attention sublayer and (2) a position-wise fully connected feed-forward network.\n\nEach sublayer is wrapped with a residual connection and then layer normalization: the sublayer output is computed as LayerNorm(x + Sublayer(x)), where Sublayer(x) denotes the function implemented by that sublayer. To make residual additions valid throughout the model, all sublayers and the embedding layers output vectors of the same dimensionality, d_model = 512.",
    "source": [
      2,
      3
    ],
    "metadata_addition": {
      "pdf_name": "sample",
      "doc_assets_path": "sample_doc_assets"
    }
  }
}